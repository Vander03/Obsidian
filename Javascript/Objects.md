
## 1 Dictionary 

```javascript
const bankAccount = {
	"Tim": 99999.9,
	"Jacob" : 150.0
};

// access with
backAccount.Tim += 10000;
Console.table(bankAccount); // shows nicely formatted table of info

for (const i in bankAccount) {} // prints the strings
for (const i of bankAccount) {} // prints the values
```

